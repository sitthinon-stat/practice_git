# Commmand Git for Start the Project

## command 
`git init` : create new Git repository on local
`git clone <ULR>` : dowlond file project form GitHub onto your Local
`git status` : check status file in your Project
